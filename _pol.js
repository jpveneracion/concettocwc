const { neon } = require("@neondatabase/serverless"); const s = neon(process.env.DATABASE_URL); (async () => { const p = await s("SELECT policyname, cmd, qual, with_check FROM pg_policies WHERE tablename = 'companies' AND cmd = 'UPDATE' ORDER BY policyname"); for (const r of p) { console.log("== " + r.policyname); console.log("CHECK: " + r.with_check); console.log(""); } })().catch(e => { console.error(e.message); process.exit(1); });
